# Public, non-secret. Baked into the production build so the Sync settings field
# pre-fills with your Worker URL (you only enter the passphrase). A localStorage
# value entered in the app overrides this.
VITE_SYNC_URL=https://planoflife-sync.gabrielschutz.workers.dev
